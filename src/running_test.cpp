#include <bits/stdc++.h>
using namespace std;

#define ll long long

#define fu(i, l, u) for (int i = l; i < u; ++i)
#define fd(i, u, l) for (int i = u - 1; i >= l; --i)

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0); 
    list<int> li;
    

    return 0;
}
